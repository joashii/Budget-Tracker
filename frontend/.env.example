# Leave empty for local dev (Vite proxies /api and /auth to localhost:4000).
# On Vercel, set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://budget-tracker-api.vercel.app
VITE_API_URL=
